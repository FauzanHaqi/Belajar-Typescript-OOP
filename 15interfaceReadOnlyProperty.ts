interface Student {
  readonly name: string;
  readonly age: number;
}

let student: Student = { name: "HAqi", age: 16 };
// student.name = "dawd";

// readonly = tidak akan bisa diubah setelah diinput 